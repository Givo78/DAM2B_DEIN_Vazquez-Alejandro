<Window x:Class="ProyectoFigma.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="600" Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Border CornerRadius="30" Background="#FFF">
        <Grid>
            <!-- Fondo degradado -->
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FFFD9345" Offset="0"/>
                    <GradientStop Color="#FFE46300" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>

            <StackPanel VerticalAlignment="Top" Margin="60,140,60,0">
                <!-- Logo circular -->
                <Grid Width="100" Height="100" Margin="0,0,0,30">
                    <Ellipse Fill="#FF7A3C00"/>
                    <Ellipse Fill="#FFF" Width="40" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Rectangle Fill="#FF7A3C00" Width="15" Height="51" VerticalAlignment="Center" Margin="42,0,42,0"/>
                </Grid>

                <!-- Campos de usuario -->
                <TextBox Width="180" Height="30" Margin="0,0,0,10" 
                         x:Name="txtUsuario"
                         Padding="5" 
                         FontSize="13" 
                         Text="Usuario"
                         Foreground="Black"
                         Background="#FFF" 
                         BorderThickness="0" 
                         />

                <PasswordBox Width="180" Height="30" Margin="0,0,0,15"
                             x:Name="txtContraseña"
                             Padding="5" 
                             FontSize="13"
                             Foreground="Black"
                             Background="#FFF"
                             BorderThickness="0"
                            />

                <!-- Botón de login -->
                <Button Content="Login"
                    Width="80" Height="30"
                    Background="#FF7A3C00"
                    Foreground="White"
                    FontWeight="Bold"
                    BorderThickness="0"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,10"
                    Click="Login_Click"/>       


                <!-- Enlace de registro -->
                <Button Content="Registrar" Height="30" Width="100" Margin="10" Click="Registrar_Click"/>

            </StackPanel>
        </Grid>
    </Border>
</Window>
